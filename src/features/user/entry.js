import { Box, CircularProgress } from "@mui/material";

const Entry = () => {
    return ( <>
    Entry❤️ pleas wait
    <Box sx={{ display: 'flex' }}>
            <CircularProgress />
        </Box></> );
}
 
export default Entry;